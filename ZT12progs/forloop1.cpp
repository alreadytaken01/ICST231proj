/*

	for while
	syntax:
	do
	{
		statement(s);
	} while (condition);
*/
#include <iostream>

using namespace std;

int main ()
{
	for (int h = 32; ( h > 4 ); h -= 3 )
	{
		cout << " Number is : " << h << endl;
	}

}
